apiVersion: v1
data:
  dedicatedserver.cfg: | -
    {{ .Files.Get "dedicatedserver.cfg" | indent 4 }}

kind: ConfigMap
metadata:
  name: "{{ .Chart.Name }}-map"
  labels:
    app: "{{ .Chart.Name }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"

